import { styled } from 'styled-components';

const S = {
    ContentBox: styled.div`
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
    `,
};

export default S;
