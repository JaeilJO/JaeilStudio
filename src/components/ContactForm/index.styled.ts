import { styled } from 'styled-components';

const S = {
    Form: styled.form`
        display: flex;
        width: 33rem;
        flex-direction: column;
        gap: 5rem;
    `,
    InputBox: styled.div`
        position: relative;
    `,
};

export default S;
