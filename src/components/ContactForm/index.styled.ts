import { styled } from "styled-components";

const S = {
  Form: styled.form`
    display: flex;
    width: 33rem;
    background-color: aliceblue;
    flex-direction: column;
    gap: 5rem;
  `,
};

export default S;
