import { styled } from 'styled-components';

const S = {
    Form: styled.form`
        display: flex;
        width: 33rem;

        flex-direction: column;
        gap: 5rem;
        background-color: aliceblue;
    `,
};

export default S;
