import Anchor from '../atoms/Anchor';
import S from './index.styled';

function Navigation() {
    return (
        <S.Navigation>
            <Anchor text={''} href={''} font_size={'sub_title_01'} font_weight={'light'} />
        </S.Navigation>
    );
}

export default Navigation;
