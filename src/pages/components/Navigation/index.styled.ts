import { styled } from 'styled-components';

const S = {
    Navigation: styled.nav`
        display: flex;
        gap: 2em;
        align-items: center;
        height: 100%;
    `,
};

export default S;
