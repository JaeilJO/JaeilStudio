import { styled } from 'styled-components';

const S = {
    Header: styled.header`
        padding: 0 40px;
        display: flex;
        align-items: center;
        justify-content: space-between;
    `,
};

export default S;
